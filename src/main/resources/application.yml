
---
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/love5757/guide-config.git
  profiles: dev

---

server:
  port: 9999

spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/love5757/guide-config.git
  profiles: prod